{
  "name": "codex-ollama-bridge",
  "version": "0.1.16",
  "private": true,
  "description": "cob: loopback bridges so Codex and Claude Code can keep native subscription models and route ollama/* to local Ollama.",
  "type": "module",
  "bin": {
    "cob": "./dist/cli.js"
  },
  "files": [
    "dist/*.js",
    "!dist/*.test.js",
    "!dist/*.harness.js",
    "!dist/gate6h.js",
    "!dist/eval-*.js",
    "README.md",
    "CHANGELOG.md",
    "RELEASE.md",
    "LICENSE",
    "NOTICE"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "tsc && node --test dist/*.test.js",
    "cob": "node dist/cli.js",
    "gate6h": "tsc && node dist/gate6h.harness.js",
    "pack": "npm run build && npm pack"
  },
  "engines": {
    "node": ">=22"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^24.3.0",
    "typescript": "^5.9.2"
  }
}
